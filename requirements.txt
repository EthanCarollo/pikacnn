# JAX — for GPU, replace with: jax[cuda12]
jax[cpu]
flax>=0.10.0
optax>=0.2.0

# Optional fallback: only needed if data/pokemon is removed and you want
# train.py to download the full-resolution Kaggle copy instead
kagglehub>=0.3.0

# Data processing
pillow>=10.0.0
numpy>=1.24.0

# Training
tqdm>=4.65.0

# Checkpointing
orbax-checkpoint>=0.5.0
