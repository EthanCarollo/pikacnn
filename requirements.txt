# JAX — for GPU, replace with: jax[cuda12]
jax[cpu]
flax>=0.10.0
optax>=0.2.0

# Dataset
kagglehub>=0.3.0

# Data processing
pillow>=10.0.0
numpy>=1.24.0

# Training
tqdm>=4.65.0

# Checkpointing
orbax-checkpoint>=0.5.0
